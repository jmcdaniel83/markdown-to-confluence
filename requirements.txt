# Core dependencies for Confluence Markdown Converter
requests>=2.31.0
markdown>=3.5.0
python-frontmatter>=1.0.0

# Additional markdown extensions for better conversion
markdown-it-py>=3.0.0
mdit-py-plugins>=0.4.0
python-markdown-math>=0.9.0
markdown-strikethrough>=0.1.2
# Definition list support via markdown.extensions.def_list (built-in)

# Optional: For better table support
tabulate>=0.9.0

# Optional: For syntax highlighting
pygments>=2.17.0

# Development dependencies (optional)
pytest>=7.4.0
black>=23.0.0
flake8>=6.0.0 